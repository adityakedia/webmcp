import { useEffect, useMemo, useRef, useState } from 'react';
import type React from 'react';
import { Box, ChevronDown, CircuitBoard, Headphones, Pause, Play, Plus, Radio, Ruler, SlidersHorizontal, Volume2, Waves, Weight, X } from 'lucide-react';
import type { CustomSpeakerConfiguration, SimulationResult } from '@acoustom/types';
import type { Product } from '../App'; import { apiUrl } from '../lib/api'; import { readLocalBuilds, type LocalBuild } from '../lib/localBuilds'; import { renderSimulatedWav } from '../lib/simulatedAudio';
type Props={products:Product[];onBack:()=>void;onPreview:(p:Product)=>void};type Profile={status:'reference_ready'|'component_model_ready';referenceId:'two_way_compact'|'two_way_extended'|'three_way_reference'|'subwoofer_active';modelInputs?:unknown};type Speaker={id:string;name:string;type:string;image:string;price:number;specs:[string,string][];product?:Product;configuration?:CustomSpeakerConfiguration;profile?:Profile};type AudioState={status:string;url?:string};
const room={width:5,length:4,height:2.7,presetId:'living_room'},positions=[{x:1.2,y:.8,z:.6,rotation:15},{x:3.8,y:.8,z:.6,rotation:-15}],listener={x:2.5,y:3.2,z:1.1};const usd=(x:string|number)=>`$${(typeof x==='number'?x:Number(x.replace(/[^0-9.]/g,''))||0).toLocaleString('en-US')}`;
const icon=(m:string)=>m.includes('Frequency')?<Waves/>:m.includes('Sensitivity')||m.includes('SPL')?<Volume2/>:m.includes('impedance')||m.includes('Crossover')?<CircuitBoard/>:m.includes('Dimensions')?<Ruler/>:m.includes('Weight')?<Weight/>:m.includes('Cabinet')?<Box/>:m==='System'?<Radio/>:<SlidersHorizontal/>;
const DIMENSIONS:Record<string,Record<string,string>>={standmount:{compact:'360 × 200 × 280 mm',standard:'440 × 215 × 360 mm',large:'520 × 250 × 430 mm'},floorstanding:{compact:'700 × 250 × 300 mm',standard:'900 × 290 × 310 mm',large:'1180 × 290 × 390 mm'},subwoofer:{compact:'320 × 320 × 360 mm',standard:'380 × 380 × 420 mm',large:'450 × 450 × 480 mm'}};
const WEIGHT:Record<string,Record<string,string>>={standmount:{compact:'11 kg each',standard:'16 kg each',large:'24 kg each'},floorstanding:{compact:'18 kg each',standard:'26 kg each',large:'40 kg each'},subwoofer:{compact:'8 kg each',standard:'12 kg each',large:'16 kg each'}};
const ampPower=(s?:number)=>!s?'—':s<85?'50 – 150 W':s<88?'40 – 180 W':s<90?'40 – 200 W':'30 – 250 W';
const pretty=(s:string)=>s.replaceAll('_',' ');
const derive=(b:any):[string,string][]=>{const d=b.derived,p=d.simulationProfile,a=d.acousticDesign,c=d.physicalBuild;const isSub=c.format==='subwoofer';return[['System',p.referenceName],['Frequency response',`${p.frequencyRangeHz[0]} Hz – ${p.frequencyRangeHz[1]} Hz`],['Sensitivity',p.sensitivityDb?`${p.sensitivityDb} dB (2.83 V / 1 m)`:'—'],['Nominal impedance',p.nominalImpedanceOhm?`${p.nominalImpedanceOhm} Ω`:'—'],['Recommended amplifier power',ampPower(p.sensitivityDb)],['Crossover frequency',p.crossoverHz.length?p.crossoverHz.join(' / ')+' Hz':'—'],['Maximum SPL',p.maxSplDb?`${p.maxSplDb} dB at 1 m`:'—'],['Amplification',isSub?'Built-in class-D amplification':'Passive (external amplification required)'],['Cabinet materials','MDF with internal bracing'],['Cabinet finishes',pretty(c.finish)],['Grille',pretty(c.grille)],['Base',pretty(c.base)],['Directivity','—'],['Inputs',isSub?'Line-level RCA / Speaker-level':'Single-wired speaker terminals'],['Calibration','—'],['Dimensions (H × W × D)',DIMENSIONS[c.format]?.[c.cabinetSize]??'—'],['Weight',WEIGHT[c.format]?.[c.cabinetSize]??'—'],['Alignment',pretty(a.alignment)],['Bass character',pretty(a.bassCharacter)],['Net volume',a.netVolumeLitres?`${a.netVolumeLitres} L`:'—'],['Port tuning',a.portTuningHz?`${a.portTuningHz} Hz`:'Sealed']]};
function Player({source,label}:{source?:string;label:string}){const ref=useRef<HTMLAudioElement>(null);const[playing,setPlaying]=useState(false);return <div className="matrix-audio"><audio ref={ref} src={source} onEnded={()=>setPlaying(false)}/><button disabled={!source} onClick={()=>{if(!ref.current||!source)return;if(ref.current.paused){void ref.current.play();setPlaying(true)}else{ref.current.pause();setPlaying(false)}}}>{playing?<Pause size={16}/>:<Play size={16}/>}</button><span>{label}</span><Volume2 size={15}/></div>}
export default function ComparisonPage({products,onBack}:Props){const [saved,setSaved]=useState<LocalBuild[]>(()=>readLocalBuilds()?.builds??[]);useEffect(()=>{const refresh=()=>setSaved(readLocalBuilds()?.builds??[]);window.addEventListener('acoustom-builds-updated',refresh);return()=>window.removeEventListener('acoustom-builds-updated',refresh);},[]);const[derived,setDerived]=useState<Record<string,{specs:[string,string][];profile:Profile}>>({});const[scrolled,setScrolled]=useState(false);const matrixRef=useRef<HTMLElement>(null);const catalog:Speaker[]=products.map(p=>({id:`catalog:${p.name}`,name:p.name,type:p.type,image:p.image,price:Number(p.price.replace(/[^0-9.]/g,''))||0,specs:p.specs,product:p}));useEffect(()=>{let live=true;void Promise.all(saved.map(async b=>{try{const r=await fetch(apiUrl('/api/custom-speakers/'),{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(b.configuration)});if(!r.ok)throw Error();const build=await r.json();return[b.id,{specs:derive(build),profile:build.derived.simulationProfile as Profile}]as const}catch{return null}})).then(rows=>{if(live)setDerived(Object.fromEntries(rows.filter((x):x is readonly[string,{specs:[string,string][];profile:Profile}]=>!!x)))});return()=>{live=false}},[saved]);const custom:Speaker[]=saved.map(b=>({id:`custom:${b.id}`,name:b.name,type:'Custom design',image:'/images/components/platform-compact.png',price:0,configuration:b.configuration,specs:derived[b.id]?.specs??[['System','Deriving build profile…']],profile:derived[b.id]?.profile}));const choices=[...catalog,...custom];const[slots,setSlots]=useState<(Speaker|null)[]>([catalog[0]??null,null,null,null,null]);const[chooser,setChooser]=useState<number|null>(null);const[audio,setAudio]=useState<Record<string,AudioState>>({});const selected=useMemo(()=>slots.filter((x):x is Speaker=>!!x),[slots]);const metrics=useMemo(()=>Array.from(new Set(selected.flatMap(s=>s.specs.map(x=>x[0])))),[selected]);const setSlot=(i:number,s:Speaker|null)=>{setSlots(all=>all.map((x,n)=>n===i?s:x));setChooser(null)};const available=(i:number)=>choices.filter(s=>!slots.some((x,n)=>n!==i&&x?.id===s.id));useEffect(()=>{if(!selected.length&&catalog[0])setSlots([catalog[0],null,null,null,null])},[catalog,selected.length]);useEffect(()=>{const onScroll=()=>{const m=matrixRef.current;if(m)setScrolled(m.getBoundingClientRect().top<120)};window.addEventListener('scroll',onScroll,{passive:true});onScroll();return()=>window.removeEventListener('scroll',onScroll)},[]);useEffect(()=>{let dead=false;const abort=new AbortController(),urls:string[]=[];const run=async()=>{const source=await fetch('/wav/sample-15s.wav');const track=new File([await source.blob()],'reference.wav',{type:'audio/wav'});await Promise.all(selected.map(async s=>{if(!s.product&&!s.profile)return;setAudio(a=>({...a,[s.id]:{status:'Generating simulation…'}}));try{const request=s.product?{speakerId:s.product.name,room,speakers:positions,listener}:{speakerId:'custom-reference',speakerProfile:s.profile,room,speakers:positions,listener};const response=await fetch(apiUrl('/api/simulate'),{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(request),signal:abort.signal});if(!response.ok)throw Error();setAudio(a=>({...a,[s.id]:{status:'Rendering audio…'}}));const wav=await renderSimulatedWav(track,await response.json() as SimulationResult);const url=URL.createObjectURL(wav);urls.push(url);if(!dead)setAudio(a=>({...a,[s.id]:{status:'Ready',url}}));}catch{if(!dead&&!abort.signal.aborted)setAudio(a=>({...a,[s.id]:{status:'Unavailable'}}))}}))};void run().catch(()=>undefined);return()=>{dead=true;abort.abort();urls.forEach(URL.revokeObjectURL)}},[selected]);const cells=(f:(s:Speaker|null,i:number)=>React.ReactNode)=>slots.map(f);return <main className="comparison-page comparison-redesign"><div className={`comparison-sticky-bar${scrolled?' is-visible':''}`}><div className="comparison-sticky-bar-inner"><div className="sticky-head">Speakers</div>{cells((s,i)=><div className="sticky-cell" key={i}>{s?<><span className="sticky-name">{s.name}</span><span className="sticky-price">{usd(s.price)} per pair</span></>:null}</div>)}</div></div><button className="back-link" onClick={onBack}>← Back to collection</button><header className="comparison-hero"><div><p className="eyebrow">Speaker comparison</p><h1>Hear the <em>difference.</em></h1><p>Choose up to five speakers. Every selection is tested against one room and one reference track.</p></div><div className="comparison-room-card"><span>Sound simulation</span><strong>Living room</strong><small>5.0 × 4.0 × 2.7 m</small><i><Headphones size={16}/> Same room for every speaker</i></div></header><section className="comparison-matrix" ref={matrixRef as any}><div className="matrix-row matrix-speaker-row"><div className="matrix-head"><span>Speakers</span><small>{selected.length} of 5 selected</small></div>{cells((s,i)=><div className="matrix-cell matrix-speaker" key={i}>{s?<article><button className="matrix-remove" onClick={()=>setSlot(i,null)}><X size={12}/></button><img src={s.image} alt={s.name}/><div><p>{s.type}</p><h2>{s.name}</h2><strong>{usd(s.price)} <small>per pair</small></strong></div></article>:<button className="matrix-add" onClick={()=>setChooser(i)}><Plus size={24}/><span>Add speaker</span><ChevronDown size={16}/></button>}</div>)}</div><div className="matrix-row matrix-sound-row"><div className="matrix-head"><span>Sound simulation</span><small>Original reference</small><Player source="/wav/sample-15s.wav" label="Reference track"/></div>{cells((s,i)=><div className="matrix-cell matrix-sound" key={i}>{s?(audio[s.id]?.url?<Player source={audio[s.id].url} label="Simulated response"/>:<div className="matrix-status">{audio[s.id]?.status??(s.profile||s.product?'Generating simulation…':'Deriving build profile…')}</div>):null}</div>)}</div><div className="matrix-row matrix-spec-title"><div className="matrix-head"><span>Specifications</span><small>Metric</small></div>{cells((_s,i)=><div className="matrix-cell" key={i}/>)}</div>{metrics.map(m=><div className="matrix-row matrix-metric-row" key={m}><div className="matrix-metric">{icon(m)}<span>{m}</span></div>{cells((s,i)=><div className="matrix-value" key={i}>{s?.specs.find(x=>x[0]===m)?.[1]??'—'}</div>)}</div>)}</section>{chooser!==null&&<div className="speaker-modal-backdrop" onClick={()=>setChooser(null)}><section className="speaker-modal" onClick={e=>e.stopPropagation()}><header><div><p className="eyebrow">Choose a speaker</p><h2>Add to comparison</h2></div><button onClick={()=>setChooser(null)}><X size={18}/></button></header><div className="speaker-modal-list">{available(chooser).map(s=><button key={s.id} onClick={()=>setSlot(chooser,s)}><img src={s.image} alt=""/><span><strong>{s.name}</strong><small>{s.type} · {usd(s.price)}</small></span><Plus size={18}/></button>)}</div></section></div>}</main>}
